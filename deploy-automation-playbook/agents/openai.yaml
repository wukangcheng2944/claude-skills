interface:
  display_name: "Deploy Automation Playbook"
  short_description: "Design safe CI/CD, rollback, HA, and step-level deploy logging."
  default_prompt: "Use $deploy-automation-playbook to design or harden an automated deployment workflow with rollback, health checks, branch-safety guardrails, and detailed per-step deployment logs."

policy:
  allow_implicit_invocation: true

